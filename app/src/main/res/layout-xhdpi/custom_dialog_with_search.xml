<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="290dp"
  android:background="#FFF">

  <LinearLayout
    android:id="@+id/search_dialog_title_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:background="@color/top_bar_color"
    android:padding="10dp">

    <test.com.thinksity.util.HeaderText
      android:id="@+id/message_top_bar_store_txt"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:gravity="start"
      android:paddingStart="5dp"
      android:text="@string/choose_country" />
  </LinearLayout>

  <EditText
    android:id="@+id/searchString"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_below="@+id/search_dialog_title_bar"
    android:gravity="center_vertical"
    android:hint="@string/search_here"
    android:padding="5dp"
    android:textSize="15sp" />

  <ListView
    android:id="@+id/search_dialog_listview"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_below="@+id/searchString"
    android:divider="#D2D2D2"
    android:dividerHeight="0.5dp"></ListView>

</RelativeLayout>