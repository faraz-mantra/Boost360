<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/staff_list_swipe_refresh"
            android:layout_width="match_parent"
            app:swipeRefreshLayoutProgressSpinnerBackgroundColor="@color/orange"
            android:layout_height="wrap_content">

            <include
                android:id="@+id/layout_staff_listing"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                layout="@layout/layout_staff_listing"
                android:visibility="gone" />
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <include
            android:id="@+id/fragment_staff_add"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            layout="@layout/fragment_staff_add"
            android:visibility="gone" />
    </LinearLayout>
</layout>