package com.dashboard.holder.v2

import com.dashboard.databinding.ItemTodoListActionBinding
import com.dashboard.recyclerView.AppBaseRecyclerViewHolder
import com.dashboard.recyclerView.BaseRecyclerViewItem

class MyToDoActionItemViewHolder(binding: ItemTodoListActionBinding) : AppBaseRecyclerViewHolder<ItemTodoListActionBinding>(binding){

    override fun bind(position: Int, item: BaseRecyclerViewItem) {
        super.bind(position, item)
    }
}
